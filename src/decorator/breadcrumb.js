// import React from 'react';

/**
 * Add this decorator to a class to change the breadcrumbs of the page.
 */
const breadcrumb = () => WrappedComponent => WrappedComponent;

export default breadcrumb;
